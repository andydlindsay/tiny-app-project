<!DOCTYPE html>
<html lang="en">

  <% include ./partials/_header %>

  <body>

    <h1>Your Short URLs</h1>

    <table>
      <% for(let url in urlDatabase) { %>
        <tr>
          <td><a href="#">http://localhost:8080/<%= url %></a></td>
          <td>(<%= urlDatabase[url] %>)</td>
        </tr>
      <% } %>
    </table>

    <a href="/urls/new">Shorten a URL</a>
  </body>

</html>
